import 'package:flutter/material.dart';
import 'package:zipper/screens/mapscreens/maps.dart'; // Adjust the import path accordingly

class HomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: MapScreen());
  }
}